import React from "react"
import logo from '../images/logo.png'

function Logo() {
  return <div className="logo">
    <img src={logo} width={75} alt="nati natash logo" />
  </div>
}

export default Logo
